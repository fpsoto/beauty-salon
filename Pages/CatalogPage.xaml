<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Beauty_Salon.Pages.CatalogPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:Beauty_Salon.ViewModels"
    xmlns:catalog="clr-namespace:BeautySalon.Application.Features.Catalog;assembly=BeautySalon.Application"
    xmlns:strings="clr-namespace:Beauty_Salon.Resources.Strings"
    x:DataType="viewModels:CatalogViewModel"
    Title="{x:Static strings:AppResources.CatalogTitle}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{x:Static strings:AppResources.AddCategory}" Clicked="OnAddCategoryClicked" />
        <ToolbarItem Text="{x:Static strings:AppResources.AddService}" Clicked="OnAddServiceClicked" />
    </ContentPage.ToolbarItems>

    <RefreshView IsRefreshing="{Binding IsBusy}" Command="{Binding LoadCommand}">
        <CollectionView
            x:Name="CatalogCollectionView"
            ItemsSource="{Binding Groups}"
            IsGrouped="True"
            SelectionMode="Single"
            SelectionChanged="OnServiceSelected">

            <CollectionView.GroupHeaderTemplate>
                <DataTemplate x:DataType="viewModels:CategoryServiceGroup">
                    <Grid ColumnDefinitions="6,*,Auto" Padding="12,8" BackgroundColor="{AppThemeBinding Light={StaticResource Background}, Dark={StaticResource BackgroundDark}}" ColumnSpacing="10">
                        <BoxView Grid.Column="0" Color="{Binding Category.ColorHex, Converter={StaticResource HexToColorConverter}}" WidthRequest="6" />
                        <Label Grid.Column="1" Text="{Binding Category.Name}" Style="{StaticResource H3}" VerticalOptions="Center" />
                        <Label Grid.Column="2" Text="&#8942;" FontSize="20" VerticalOptions="Center">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnCategoryOptionsTapped" CommandParameter="{Binding Category}" />
                            </Label.GestureRecognizers>
                        </Label>
                    </Grid>
                </DataTemplate>
            </CollectionView.GroupHeaderTemplate>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="catalog:SalonServiceDto">
                    <Border Style="{StaticResource Card}" Margin="16,4">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding Name}" />
                                <Label Text="{Binding DurationMinutes, StringFormat='{0} min'}" Style="{StaticResource Caption}" />
                            </VerticalStackLayout>
                            <Label Grid.Column="1" Text="{Binding SuggestedPrice, StringFormat='${0}'}" Style="{StaticResource FieldCaption}" VerticalOptions="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>
